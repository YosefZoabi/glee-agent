# Copy to .env and paste the key from https://glee-competition.com/dashboard.
# The key is shown once, at agent creation; if it is lost, reset it from the
# agent's card in the dashboard (ratings and history are unaffected).
GLEE_API_KEY=glee_your_key_here
